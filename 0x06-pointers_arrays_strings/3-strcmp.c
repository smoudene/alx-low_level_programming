#include "main.h"

/**
 * _strcmp - compares two strings
 *
 * @s1: pointer of first string
 * @s2: ponter of second string
 *
 * Return: the diffrence between the strings
 */
int _strcmp(char *s1, char *s2)
{
	return (*s1 - *s2);
}
