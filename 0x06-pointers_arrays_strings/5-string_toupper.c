#include "main.h"

/**
 * *string_toupper - make string in uppercase
 * @s: string
 * Return: string in upper case
 */
char *string_toupper(char *s)
{
	int i = 0;

	while (s[i])
	{
		if (s[i] >= 'a' && s[i] <= 'z')
		{
			s[i] = s[i] - 32;
		}
		i++;
	}
	return (s);
}
